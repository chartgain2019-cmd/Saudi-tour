# إعدادات الخادم
PORT=3000
NODE_ENV=production

# إعدادات الأمان
SESSION_SECRET=change_this_to_a_random_secret_string
JWT_SECRET=another_random_secret_string

# إعدادات قاعدة البيانات (للإصدارات المستقبلية)
# DATABASE_URL=mongodb+srv://username:password@cluster.mongodb.net/saudi-tour